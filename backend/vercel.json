{
  "version": 2,
  "builds": [
    {
      "src": "index.py",
      "use": "@vercel/python",
      "config": {
        "pythonVersion": "3.12",
        "installCommand": "python3 -m pip install --break-system-packages -r requirements.txt || pip install --user -r requirements.txt || echo 'Installation skipped'"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "index.py"
    }
  ]
}